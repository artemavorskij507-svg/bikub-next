<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>{{ $order->order_number }}</title><style>body{font:16px system-ui;max-width:900px;margin:auto;padding:24px;background:#f4f7fb}.card{background:#fff;border:1px solid #dce3ec;padding:18px;margin:12px 0;border-radius:8px}.meta{color:#64748b}</style></head><body><a href="{{ route('account.orders.index') }}">Back to orders</a><h1>{{ $order->order_number }}</h1><div class="card"><p>Status: <strong>{{ str($order->status->value)->replace('_',' ')->title() }}</strong></p><p>Service: {{ $order->scenario?->title ?? $order->service_scenario_key }}</p><p>Estimate: {{ $order->latestPriceQuote()?->total ? number_format((float)$order->latestPriceQuote()->total,2).' '.$order->currency : 'Manual review' }}</p><p>Payment provider: not connected</p><p>Customer live tracking: not exposed</p></div><div class="card"><h2>Support</h2><p>{{ $order->supportTickets->count() }} customer-linked ticket(s)</p>@foreach($order->supportTickets as $ticket)<p><a href="{{ route('account.support.show',$ticket) }}">{{ $ticket->ticket_number }} · {{ $ticket->subject }}</a></p>@endforeach</div></body></html>
