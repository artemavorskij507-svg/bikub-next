@extends('layouts.account-shell')
@section('title','Billing')
@section('content')
<header><span class="shell-eyebrow">Customer billing</span><h1>Billing center</h1><p>Invoices linked to your verified account.</p></header><section class="shell-grid cards"><article class="shell-card"><h2>Issued invoices</h2><strong>{{ $documents->where('status','issued')->count() }}</strong></article><article class="shell-card"><h2>Issued amount</h2><strong>{{ number_format((float)$issuedAmount,2) }} NOK</strong></article><article class="shell-card"><h2>Paid amount</h2><strong>{{ number_format((float)$paidAmount,2) }} NOK</strong></article><article class="shell-card"><h2>Payment provider</h2><strong>Unavailable</strong></article></section><section class="shell-grid">@forelse($documents as $document)<article class="shell-card"><h2>{{ $document->document_number }}</h2><p>{{ str($document->status)->title() }} · {{ number_format((float)$document->total_amount,2) }} {{ $document->currency }}</p><p>Order {{ $document->order?->order_number ?? 'Not linked' }} · Issued {{ $document->issued_at?->format('d M Y') ?? 'Not issued' }}</p><a href="{{ route('account.billing.documents.show',$document) }}">View invoice</a></article>@empty<div class="shell-card">No billing documents yet.</div>@endforelse</section>
@endsection