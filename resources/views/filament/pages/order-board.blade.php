<x-filament-panels::page><x-admin-os.module-shell class="los" label="Order Board">
<header class="los-head"><div><span class="los-label">LiveOps Suite</span><h1>Order Board</h1><p>Real lifecycle and dispatch queues. Status changes remain service-controlled.</p></div><x-admin-os.liveops-nav /></header>
<section class="los-kpis"><article class="los-kpi"><span>Total orders</span><strong>{{$total}}</strong></article><article class="los-kpi"><span>Columns</span><strong>{{count($columns)}}</strong></article><article class="los-kpi"><span>Movement</span><strong>Lifecycle actions</strong></article><article class="los-kpi"><span>Drag/drop</span><strong>Disabled safely</strong></article></section>
<div class="los-board">@foreach($columns as $name=>$orders)<section class="los-panel los-column"><header><h2>{{$name}}</h2><b>{{$orders->count()}}</b></header><div class="los-column-list">@forelse($orders as $o)@php($a=$o->activeDispatchAssignment())<article class="los-card"><strong>{{$o->order_number}}</strong><span>{{$o->scenario?->title ?? $o->service_scenario_key}}</span><p>{{$o->customer?->name ?? 'Customer ownership not linked'}} · {{$a?->assignedUser?->name ?? 'Unassigned'}}</p><p>{{$o->supportTickets->whereNotIn('status',['resolved','closed'])->count()}} support · {{$o->workerLocationPings->count() ? 'GPS received':'No GPS'}} · {{str($o->payment_status->value)->title()}}</p><div class="los-nav"><a href="{{\App\Filament\Resources\Orders\OrderResource::getUrl('view',['record'=>$o])}}">Order</a><a href="{{route('filament.admin.pages.order-tracking.{order}',['order'=>$o])}}">Tracking</a><a href="{{route('filament.admin.pages.dispatch-center')}}">Dispatch</a></div></article>@empty<div class="los-empty">No real orders.</div>@endforelse</div></section>@endforeach</div>
</x-admin-os.module-shell></x-filament-panels::page>
