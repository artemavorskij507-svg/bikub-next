@php($ticket=$getRecord()->load(['order','customer','workerProfile','workerDocument','dispatchAssignment','creator','assignee','messages.author','events.actor','assignments.assignee','assignments.assignedBy']))
<div class="grid gap-6">
 <x-filament::section heading="Ticket summary"><dl class="grid gap-4 md:grid-cols-4"><div><dt>Ticket</dt><dd><strong>{{$ticket->ticket_number}}</strong></dd></div><div><dt>Status</dt><dd>{{str($ticket->status)->replace('_',' ')->title()}}</dd></div><div><dt>Priority</dt><dd>{{str($ticket->priority)->title()}}</dd></div><div><dt>Category</dt><dd>{{str($ticket->category)->replace('_',' ')->title()}}</dd></div><div class="md:col-span-2"><dt>Subject</dt><dd>{{$ticket->subject}}</dd></div><div><dt>Assigned</dt><dd>{{$ticket->assignee?->name ?? 'Unassigned'}}</dd></div><div><dt>Created by</dt><dd>{{$ticket->creator?->name ?? 'System'}}</dd></div></dl></x-filament::section>
 <x-filament::section heading="Linked context"><div class="flex flex-wrap gap-4">@if($ticket->order)<a href="{{\App\Filament\Resources\Orders\OrderResource::getUrl('view',['record'=>$ticket->order])}}">Order {{$ticket->order->order_number}}</a>@endif @if($ticket->workerDocument)<a href="{{\App\Filament\Resources\WorkerDocuments\WorkerDocumentResource::getUrl('edit',['record'=>$ticket->workerDocument])}}">Worker document #{{$ticket->worker_document_id}}</a>@endif @if(!$ticket->order&&!$ticket->workerDocument)<span>No linked operational record.</span>@endif</div></x-filament::section>
 <x-filament::section heading="Messages timeline">@forelse($ticket->messages->sortByDesc('created_at') as $m)<article class="border-b py-3"><strong>{{str($m->visibility)->replace('_',' ')->upper()}}</strong> · {{$m->author?->name ?? 'System'}} · {{$m->created_at}}<p>{{$m->body}}</p></article>@empty<p>No messages.</p>@endforelse</x-filament::section>
 <x-filament::section heading="Events timeline">@forelse($ticket->events as $e)<article class="border-b py-2"><strong>{{str($e->event_type)->replace('_',' ')->title()}}</strong> · {{$e->actor?->name ?? 'System'}} · {{$e->created_at}}<p>{{$e->description}}</p></article>@empty<p>No events.</p>@endforelse</x-filament::section>
 <x-filament::section heading="Assignment history">@forelse($ticket->assignments as $a)<p>{{$a->assignee?->name}} · {{str($a->status)->title()}} · {{$a->assigned_at}} @if($a->released_at)→ {{$a->released_at}}: {{$a->release_reason}}@endif</p>@empty<p>No assignments.</p>@endforelse</x-filament::section>
</div>
