<section class="cj-card"><p class="worker-hero-eyebrow">Audit timeline</p>@php $workerEvents=$order->events->filter(fn($e)=>str_starts_with($e->event_type,'worker.')); @endphp @forelse($workerEvents as $event)<div class="kv"><span>{{ str($event->event_type)->replace(['worker.','_'],['',' '])->title() }}</span><strong>{{ $event->created_at?->format('Y-m-d H:i') }}</strong></div>@empty<p class="muted">No worker progress recorded yet.</p>@endforelse</section>