<section class="cj-card"><p class="worker-hero-eyebrow">Order details</p><div class="kv"><span>Customer</span><strong>{{ $order->customer_name }}</strong></div><div class="kv"><span>Contact</span><strong>{{ $order->customer_phone ?: $order->customer_email }}</strong></div><div class="kv"><span>Payment</span><strong>{{ str($order->payment_status->value)->replace('_',' ')->title() }}</strong></div>@forelse($intake as $key=>$value)<div class="kv"><span>{{ str($key)->replace('_',' ')->title() }}</span><strong>{{ is_array($value) ? json_encode($value) : (is_bool($value) ? ($value?'Yes':'No') : $value) }}</strong></div>@empty<p class="muted">No intake data captured.</p>@endforelse</section>