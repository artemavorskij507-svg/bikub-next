<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>Become a BiKuBe worker</title></head><body><main><h1>Apply to work with BiKuBe</h1><p>Submitting does not create or approve a worker profile.</p><form method="post" action="{{ route('public.workers.store') }}">@csrf
@foreach(['name'=>'Name','email'=>'Email','phone'=>'Phone','desired_service_area'=>'Desired service area','vehicle_type'=>'Vehicle type'] as $n=>$l)<label>{{ $l }}<input name="{{ $n }}" value="{{ old($n) }}" {{ in_array($n,['name','email'])?'required':'' }}></label>@endforeach
<label>Worker type<select name="worker_type"><option value="courier">Courier</option><option value="worker">Service worker</option><option value="driver">Driver</option></select></label>
<fieldset><legend>Capabilities</legend>@foreach(['delivery','moving','eco','handyman','towing','errands'] as $c)<label><input type="checkbox" name="capabilities[]" value="{{ $c }}">{{ ucfirst($c) }}</label>@endforeach</fieldset>
<label>Experience notes<textarea name="experience_notes">{{ old('experience_notes') }}</textarea></label><label><input type="checkbox" name="compliance_acknowledged" value="1" required>I understand approval and compliance review are required.</label>
@if($errors->any())<p>{{ $errors->first() }}</p>@endif<button>Submit application</button></form></main></body></html>
