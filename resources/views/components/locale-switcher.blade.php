<form method="POST" action="{{ route('locale.update') }}" class="inline-flex items-center gap-2">@csrf<label for="bikube-locale" class="sr-only">{{ __('bikube.localization.choose_language') }}</label><select id="bikube-locale" name="locale" onchange="this.form.submit()" aria-label="{{ __('bikube.localization.choose_language') }}" class="rounded border px-2 py-1">@foreach(config('bikube_locales.supported',[]) as $code=>$name)<option value="{{ $code }}" @selected(app()->getLocale()===$code)>{{ $name }}</option>@endforeach</select><noscript><button type="submit">{{ __('bikube.common.save') }}</button></noscript></form>
